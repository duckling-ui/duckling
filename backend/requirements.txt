# Duckling Backend — single install for API + in-app MkDocs builds (/api/docs/build).
# From the backend directory: pip install -r requirements.txt
#
# MkDocs stack (must match mkdocs.yml plugins)
mike>=1.1.0
# mkdocs-material 9.5+ requires mkdocs 1.6; keeps pip from backtracking to mkdocs 1.5.x
mkdocs>=1.6,<2
mkdocs-material>=9.5.0
mkdocs-minify-plugin>=0.8.0
mkdocs-static-i18n>=1.3.0
pymdown-extensions>=10.21.2

# Core backend
flask==3.1.3
psutil>=5.9.0
flask-cors==6.0.0
docling>=2.118.0  # latest stable; OcrMode/scale (2.116+); DocLang export needs core with export_to_doclang
docling-core>=2.90.0,<3.0.0  # DocLang 0.7 serializer; docling 2.118 pins core>=2.90,<3
sqlalchemy==2.0.36
python-dotenv>=1.1.0,<2
PyYAML>=6.0.2
# Lightweight queue client for local/RQ orchestration modes (no Ray Java runtime).
rq>=1.16.2
redis>=5.0.8
pytest>=8.3.5,<9
pytest-cov>=5.0.0,<7
werkzeug==3.1.6
gunicorn==23.0.0
# pymdown-extensions 10.21.2+ requires markdown>=3.6 (was 3.5.1; conflict broke pip on CI)
markdown>=3.6,<4
# Image-scan hardening (Trivy): minimum-safe versions; Docker build enforces upgrades.
jaraco.context>=6.1.0
wheel>=0.46.2
