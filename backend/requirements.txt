# Duckling Backend Dependencies

# MkDocs + docs (needed for /api/docs/build when running without Docker)
mkdocs>=1.5.0
mkdocs-material>=9.5.0
mkdocs-minify-plugin>=0.8.0
mkdocs-static-i18n>=1.3.0
pymdown-extensions>=10.0

# Core backend
flask==3.1.3
psutil>=5.9.0
flask-cors==6.0.0
docling>=2.50.0  # 2.50.0+ required for enrichment models (picture classification, description, formula recognition, code detection)
sqlalchemy==2.0.36
python-dotenv==1.0.0
pytest==7.4.3
pytest-cov==4.1.0
werkzeug==3.1.6
gunicorn==23.0.0
markdown==3.5.1
