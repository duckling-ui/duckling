# MkDocs-only dependencies (CI, ./scripts/docs-build.sh, contributors who build docs without the full backend).
# Running the API + in-app docs build: use backend/requirements.txt only (includes this stack + Flask/Docling).
# Keep package lines in sync with the MkDocs block in backend/requirements.txt.
# Install with: pip install -r requirements-docs.txt

mike>=1.1.0
mkdocs>=1.5.0
mkdocs-material>=9.5.0
mkdocs-minify-plugin>=0.8.0
mkdocs-static-i18n>=1.3.0
pymdown-extensions>=10.0

